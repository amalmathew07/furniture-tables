{
  "name": "code",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "install": "cd backend && npm install && cd ui && npm install && cd ../.. && cd frontend && npm install",
    "start-frontend": "npm run start --prefix ./frontend",
    "start-backend": "npm run start --prefix ./backend"
  },
  "author": "",
  "license": "ISC"
}
